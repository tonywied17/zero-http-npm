/**
 * @module sse
 * @description Server-Sent Events support for zero-server.
 *              Exports the SSEStream class.
 */
const SSEStream = require('./stream');

module.exports = { SSEStream };
